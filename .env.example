# Copy to `.env` and fill values. Do not commit `.env`.
#
# Server
PORT=8080
# CORS_ORIGIN=https://app.example.com
# PUBLIC_BASE_URL=https://app.example.com
# FORCE_SECURE_COOKIES=1
#
# Database (choose one)
# DATABASE_URL=postgres://user:pass@host:5432/dbname
# DB_PATH=./storage/app.sqlite
#
# Auth secrets (generate strong random values)
ADMIN_SESSION_SECRET=change_me_admin_secret
EXAMINER_SESSION_SECRET=change_me_examiner_secret
#
# File storage (defaults to ./storage if unset)
# FILE_STORAGE_DIR=./storage
#
# Public file storage (simple + no auth): serve stored files directly as static URLs.
# Recommended if you accept that anyone with the link can access them.
#
# FILE_STORAGE_DIR=./public/storage
# FILE_STORAGE_PUBLIC=1
# FILE_STORAGE_PUBLIC_PREFIX=/storage
#
# Proctoring / snapshots (Nextcloud optional)
# SNAPSHOT_MAX=250
# PROCTORING_MODE=on
# REQUIRE_PROCTORING_ACK=1
# PROCTORING_NOTICE_VERSION=2026-01-01
# PROCTORING_PRIVACY_NOTICE_URL=https://app.example.com/privacy
# PROCTORING_RETENTION_DAYS=30
#
# Nextcloud mirroring (optional)
# When set, every stored file is also uploaded to the shared folder via WebDAV.
# You can pass either the public share link OR the WebDAV base URL:
# NEXTCLOUD_SHARE_URL=https://nextcloud.example.com/s/SHARETOKEN
# NEXTCLOUD_SHARE_PASSWORD=
# (Advanced) NEXTCLOUD_WEBDAV_URL=https://nextcloud.example.com/public.php/webdav/
# (Advanced) NEXTCLOUD_WEBDAV_USER=SHARETOKEN
# (Advanced) NEXTCLOUD_WEBDAV_PASS=
#
# By default mirroring failures fail the request. Set to 0 for best-effort.
# NEXTCLOUD_MIRROR_REQUIRED=1
#
# Dropbox listening uploads (optional)
# DROPBOX_ACCESS_TOKEN=
# DROPBOX_LISTENING_DIR=/eng4sp_listening
# 
# Listening MP3 upload limit:
# - App accepts up to 100 MB.
# - If behind Nginx, set: client_max_body_size >= 128m.
# - If you cannot change Nginx/proxy limits, the UI uses chunked uploads automatically.
#   You can tune server-side max chunk bytes (default ~900KB):
# LISTENING_UPLOAD_CHUNK_BYTES=921600
#
# Meetings / speaking provider
# SPEAKING_PROVIDER=livekit
# MEETING_BASE_URL=https://meet.example.com
#
# LiveKit (if SPEAKING_PROVIDER=livekit)
# LIVEKIT_URL=wss://livekit.example.com
# LIVEKIT_API_URL=http://livekit:7880
# LIVEKIT_API_KEY=
# LIVEKIT_API_SECRET=
#
# Excel import: speaking meeting/link generation concurrency (default 5, max 12).
# SPEAKING_IMPORT_CONCURRENCY=5
# Excel import UI: smooth "Loaded x/y" steps (0 disables smoothing).
# IMPORT_PROGRESS_SMOOTH_STEPS=12
# Excel import DB chunk size for progress + performance (Postgres uses this; SQLite ignores and uses safe chunking).
# IMPORT_DB_CHUNK_SIZE=400
# Excel import: if you prefer 100% real "1-by-1" progress (slower), enable this.
# IMPORT_PROGRESS_PER_ROW=1
#
# Self-hosted meeting providers (optional)
# SELF_HOSTED_JITSI_BASE_URL=https://meet.example.com
# SELF_HOSTED_MIROTALK_BASE_URL=https://meet.example.com
# SELF_HOSTED_TALKY_BASE_URL=https://talky.io
# SELF_HOSTED_LIVEKIT_URL=wss://livekit.example.com
#
# Zoom (if SPEAKING_PROVIDER=zoom)
# ZOOM_ACCOUNT_ID=
# ZOOM_CLIENT_ID=
# ZOOM_CLIENT_SECRET=
# ZOOM_HOST_EMAIL=host@example.com
# ZOOM_HOST_USER_ID=
# ZOOM_TIMEZONE=UTC
